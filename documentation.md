Documentation lier a la resource Furious

Functions :

Func.GetIdentifier(source, type) :
retourne l'identifiant souhaiter selon l'id (source) du joueur (sans le préfixe)

Database :

DB.Connect() : print et boolean DB.Connected
DB.CheckConnection() : vérification de la connexion a la base de données
DB.Start() : au lancement de la resource, lance la function DB.Connect()
